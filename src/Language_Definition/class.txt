#Use class keyword to create a class, and each class will have a default constructor
class mySchool{
    arr[num] scores;
    arr[str] classes;
    bool enrolled;

    #The constructor will initialize all variables with empty values
    func str School(){
        scores = [];
        classes = [];
        enrolled = False;

        str a = "Hello World";
        result(a);

        dec b = 2.2 - 2.4;
        return "Add new school";
    }
}