arr[num] points = [5, 10, 12, 4, 7, 2];

points.add(17);

points.rm(4);

f_loop(num i = 0 to 6) exist{
    if(i > points[i]) exist{
        result("Small Points");
    }else{
        result("Next points");
    }
}